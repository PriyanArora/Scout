name: deploy

on:
  push:
    branches:
      - main

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Explain placeholder
        run: |
          set -euo pipefail
          echo "Supabase and Vercel deployment steps are added in P17."
