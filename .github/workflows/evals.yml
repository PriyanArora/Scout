name: evals

on:
  workflow_dispatch:
  schedule:
    - cron: "0 9 * * *"

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Explain placeholder
        run: |
          set -euo pipefail
          echo "Eval fixtures and LLM-judge checks are added in P16."
